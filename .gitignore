node_modules/
dist/
*.log

# secrets — never committed
.env
.env.*
!.env.example
# Local-only Postgres credentials and a set of deliberately empty values that
# keep a test run off the network. No secrets, and CI needs it in the checkout.
!packages/server/.env.test

# runtime state
backups/

# expo
.expo/

# native projects are generated from app.json by `expo prebuild` (CNG) —
# regenerate them, don't edit or commit them
packages/app/android/
packages/app/ios/

